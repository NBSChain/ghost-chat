<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    >
    <!-- Twitter Icon -->
    <Style x:Key="BTIcon" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="/K.Framework.Controls;component/Resources/#SF2015" />
        <Setter Property="Foreground" Value="{StaticResource Foreground}"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="20"/>
    </Style>


    <!-- 主页内容边框 -->
    <Style TargetType="{x:Type Border}" x:Key="MainBorderStyle">
        <Setter Property="CornerRadius" Value="1"/>
        <Setter Property="BorderBrush" Value="{x:Null}"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="Background">
            <Setter.Value>
                <VisualBrush TileMode="Tile" >
                    <VisualBrush.Visual>
                        <Image Source="/Theme/Images/appbg.png"></Image>
                    </VisualBrush.Visual>
                </VisualBrush>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>